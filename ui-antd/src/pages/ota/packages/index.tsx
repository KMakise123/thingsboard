/**
 * OTA packages route entry (M13 wave-2): `/otaPackages` is the packages
 * repository list; the detail page lives at `./detail` (route
 * `/otaPackages/:id`).
 */
export { default } from './list';
