/**
 * Dashboard editor re-export shim (M8 wave F hoist) — the implementation
 * lives in core/editor/contract/use-leave-guard.ts. Public API unchanged.
 */
export {
  shouldPromptLeave,
  type UseLeaveGuardOptions,
  useLeaveGuard,
} from '@/core/editor/contract/use-leave-guard';
