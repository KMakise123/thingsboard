/**
 * WidgetConfigPanel — stable entry of the right-side config panel (M7 brief
 * §2/§3). C wave froze the props seam; K wave fills the five-section panel
 * implementation (spec §3.4) behind this exact signature.
 */
export {
  type PanelSectionId,
  WidgetConfigPanel,
  type WidgetConfigPanelProps,
} from './WidgetConfigPanel';
