/**
 * rule-chains domain entry (M8 brief §3 wave C file ownership). The
 * editable surface lives in ./editor (route `/ruleChains/:ruleChainId`);
 * the list page joins in wave 3 D as ./list (route /ruleChains).
 */
export { default } from './editor';
