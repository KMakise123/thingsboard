/**
 * Asset detail page (8 tabs per spec 3.3/3.4). M2 scaffold stub.
 * Menu entry, access gating and URL live in config/routes.ts; the shell
 * header, breadcrumb and title come from the shared PageContainer wrapper
 * (ADR 0008).
 */
import PageContainer from '@/components/layout/page-container';

/** M2 scaffold stub — replaced by the domain agent. */
export default function AssetDetailPage() {
  return <PageContainer />;
}
