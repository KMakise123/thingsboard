/**
 * zh-CN seed for the widget-type domain (`editor.widget.*`, ADR 0004 §6).
 *
 * M9 wave-1 F ships contracts only (types / transport / frozen core shapes)
 * — no UI, hence the empty skeleton. Later waves append the keys they
 * actually use:
 *   - editor shell / tabs / sidebar / preview copy → `editor-widget-editor.ts`
 *     (S wave, per brief §3);
 *   - compiled-widget facade copy → `widget-kit.ts` (K wave).
 * Keep zh-CN/en-US key-for-key identical (check-locale gate).
 */
export default {
  // 'editor.widget. …': '…',
};
