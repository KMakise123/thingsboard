/**
 * en-US rule-chain editor shared base keys (`editor.ruleChain.*`, M8 wave
 * F seed) — same contract as zh-CN/editor-rulechain.ts, key-for-key.
 */
export default {
  // §2 checkpoint semantics — the save() checkpoint notice
  'editor.ruleChain.checkpointCleared':
    'Saved — the undo history has been cleared',
};
