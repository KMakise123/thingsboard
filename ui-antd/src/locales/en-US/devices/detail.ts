/**
 * en-US device detail keys. Content is owned by the devices wave; the shell
 * wave only created the file so the aggregators stay stable. Must stay
 * key-for-key identical with zh-CN/devices/detail.ts (check-locale gate).
 */
export default {};
