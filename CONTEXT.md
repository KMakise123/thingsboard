# 领域词汇表

> 单上下文布局：本文件是全仓库唯一词汇表（glossary），只收术语定义，不收实现细节；架构决策见 docs/adr/，wayfinder 决议见 GitHub Issues。

## 产品与范围

- **v1（对齐切片）**：前端重写第一阶段——除编辑器三件套与八个子系统（通知中心、Edge、OTA、版本控制独立页、资源库、计算字段独立页、mobile-center、iot-hub）外，对齐 ui-ngx 全部页面，能力逐项等价、不做简化版；三角色（SYS_ADMIN / TENANT_ADMIN / CUSTOMER_USER）全支持。原「最小运维切片」口径由 #9 废弃。
- **全功能对齐（v2）**：第二阶段——补齐编辑器三件套与八个子系统，对齐旧前端全部功能；登录后首页调整为 home dashboard。
- **编辑器三件套（v2）**：仪表盘编辑器、widget 编辑器、规则链画布三个画布类编辑器的合称；v1 页面中跳转它们的入口一律隐藏或禁用。
- **一步切换**：新前端就绪后直接整体替换旧前端 ui-ngx，无双 UI 共存或灰度过渡（系统未上线）。
- **休眠保留**：ui-ngx 在切换后的状态——目录留在工作树作对照素材，但移出 Maven 构建链（不构建、不进产物）；v1 验收后复审是否删除。
- **自有版本（fork 姿态）**：本 fork 是自有产品——不跟随上游功能演进，仅跟进安全补丁；改动走独立模块/目录，不 inline 改上游代码。

## 前端蓝图

- **品牌接缝（BrandConfig）**：品牌事实的唯一来源——品牌 token、图表分类色板与品牌资产（logo/标题/favicon 等）的集合；white-labeling 换品牌只改这一处。
- **遥测订阅管理器**：面向单 socket 多路复用 WS 协议的订阅引擎——以命令族（时序/属性/实体数据/告警/通知）为单位管理订阅生命周期、断线重连与重订阅；订阅数据不进 REST 请求缓存，REST 快照只作单向种子。
- **内置 widget 注册表**：仪表盘 widget 类型全限定名到受控 React 组件的映射面；解析链分四态——内置命中 / 自定义编译注册（`runtime: 'react-1'`）/ Angular 永久不支持占位 / 类型缺失占位，占位逻辑收敛在注册表层。
- **聚合文件约定**：语言包目录化经手写聚合文件实现（聚合文件引入各域文件后合并导出），而非构建工具原生的目录扫描。
- **api-client 单一出口缝**：前端所有 HTTP 请求的唯一出口——组件层禁止裸 fetch，认证注入、错误规范化与刷新重放都在这一层收口。
- **登记册（BCR）**：后端契约缺口的例外登记——一条契约一个条目，绑定消费功能、不实现时的 fallback 与权限镜像源；在阶段边界集中复审。
- **最小回归基线（regression baseline）**：v1 gate 的可执行回归面——横切专项、三角色冒烟矩阵、core 单测集与视觉锚点四件全绿即达标；域内操作级细项不在其中，保持人工 checklist 勾选（混合模式）。
- **冒烟矩阵（smoke matrix）**：每域一条最短路径（列表 → 搜索 → 详情 → tab → 返回）× 三角色的 E2E 网格，随里程碑逐格点亮，M6 收口完整。
- **视觉锚点（visual anchor）**：高风险面（demo 仪表盘、登录页）的双保险断言——DOM 无占位 + 像素快照对比；基准截图入库，有意改版显式更新后提交。
- **编辑会话（EditorSession）**：编辑器打开后对一份服务端实体的受控工作副本——进会话时规范化出**草稿（draft）**，会话期间草稿有唯一写者，保存成功后**基线（baseline）**前移；「是否改过」由草稿与基线的引用比较精确判定（改了又全部撤销会自动变干净）。
- **事务组（transaction group）**：撤销栈的一等元素——一次用户意图（一次拖拽、一次粘贴、一段连续表单输入）产生的全部变更打包成一组，撤销/重做以组为单位回放；交互进行中的临时状态（拖拽中的坐标）不入栈，操作落定才成组。
- **表单配方（FormProperty）**：widget 设置与规则链节点配置共用的声明式表单描述格式（扁平属性数组 + 控件类型枚举 + 声明式显隐），与上游 ui-ngx 已迁移到的 `settingsForm` 同键同格式；渲染器统一映射到 antd 控件，复杂面板超出配方表达力时走定制组件或 JSON 源码模式。
- **资源库（Resources Library）**：五合一子系统——widget 类型库、图片库、SCADA 符号库、JS 库、资源文件库的合称；SCADA 符号无独立后端资源类型（以图片子类型存储）。独立页归 v2 后半场（M11 交付），SCADA 符号编辑器页为其最重组件；仪表盘内的符号实例只能换符号、绑设备、绑对象，SVG 结构编辑只发生在符号编辑器页。
- **SCADA 布局**：仪表盘布局类型（gridSettings.layoutType 枚举值之一，与 default / divider 并列）——零边距满铺、列数限定 24 的倍数、禁用手机断点、新增 widget 自动仪表化（去标题去阴影、透明背景、锁定宽高比）；与普通布局共用同一网格编辑交互，非独立编辑器；SVG 符号结构编辑归资源库子系统的符号编辑器页。
